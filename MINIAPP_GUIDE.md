# Руководство по запуску Telegram Mini App

## Быстрый старт

### 1. Установка зависимостей Python

```bash
# Установить FastAPI и uvicorn
uv sync
```

### 2. Настройка переменных окружения

Добавьте в `.env` файл:

```env
# Существующие переменные...
WEBAPP_URL=http://localhost  # Для разработки
# WEBAPP_URL=https://your-domain.com  # Для production
```

### 3. Запуск в режиме разработки

#### Вариант A: Отдельные процессы

```bash
# Терминал 1: Запуск API сервера
python api_server.py

# Терминал 2: Запуск Telegram бота
python main.py

# Терминал 3: Запуск Vue.js dev сервера
cd webapp
npm install
npm run dev
```

#### Вариант B: Docker Compose (рекомендуется)

```bash
# Сборка и запуск всех сервисов
docker-compose up --build

# Для пересборки только webapp
docker-compose build webapp
docker-compose up -d webapp
```

### 4. Проверка работоспособности

1. Откройте Telegram бот
2. Отправьте команду `/start`
3. Нажмите на кнопку "🛍️ Открыть каталог"
4. Должен открыться Mini App с каталогом товаров

## Архитектура

```
┌─────────────────┐
│  Telegram Bot   │  (main.py)
│   aiogram 3.x   │
└────────┬────────┘
         │
         │ WebApp button
         │
┌────────▼────────┐
│   Vue.js App    │  (webapp/)
│  Frontend SPA   │
└────────┬────────┘
         │
         │ HTTP/API
         │
┌────────▼────────┐
│  FastAPI Server │  (api_server.py)
│   REST API      │
└────────┬────────┘
         │
         │ SQLAlchemy
         │
┌────────▼────────┐
│   PostgreSQL    │
│    Database     │
└─────────────────┘
```

## Структура проекта

```
mdm-bot/
├── main.py              # Telegram Bot (aiogram)
├── api_server.py        # FastAPI REST API
├── models.py            # SQLAlchemy модели
├── database.py          # Конфигурация БД
├── kbs.py              # Клавиатуры (включая Web App кнопку)
├── config.py           # Настройки (добавлен WEBAPP_URL)
├── webapp/             # Telegram Mini App
│   ├── src/
│   │   ├── components/
│   │   │   └── ProductList.vue
│   │   ├── api/
│   │   │   └── products.js
│   │   ├── App.vue
│   │   ├── main.js
│   │   └── style.css
│   ├── index.html
│   ├── vite.config.js
│   ├── package.json
│   ├── Dockerfile
│   └── nginx.conf
└── docker-compose.yaml  # Обновлен с api и webapp сервисами
```

## API Endpoints

### GET /api/products

Получить список товаров с пагинацией.

**Query параметры:**
- `page` (int, default=1): Номер страницы
- `limit` (int, default=20, max=100): Количество товаров на странице

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "name": "Товар 1",
      "price": 1000.0,
      "image": "https://example.com/image.jpg",
      "vendor_code": "ART-001",
      "description": "Описание товара"
    }
  ],
  "total": 150,
  "page": 1,
  "limit": 20,
  "total_pages": 8
}
```

### GET /api/products/{product_id}

Получить информацию о конкретном товаре.

**Ответ:**
```json
{
  "id": 1,
  "name": "Товар 1",
  "price": 1000.0,
  "image": "https://example.com/image.jpg",
  "vendor_code": "ART-001",
  "description": "Описание товара"
}
```

### GET /api/health

Проверка состояния API.

**Ответ:**
```json
{
  "status": "ok",
  "service": "mdm-bot-api"
}
```

## Production Deployment

### 1. Настройка домена

Вам потребуется:
- Зарегистрированный домен
- SSL сертификат (Let's Encrypt)
- Настроенный веб-сервер (nginx/caddy)

### 2. Обновление .env

```env
WEBAPP_URL=https://your-domain.com
```

### 3. Обновление конфигурации бота

В [kbs.py](kbs.py:15) кнопка автоматически использует `WEBAPP_URL` из настроек.

### 4. Docker Compose для production

```bash
# Копируем production конфигурацию
cp .env.production .env

# Запускаем все сервисы
docker-compose up -d

# Проверяем статус
docker-compose ps
docker-compose logs -f webapp
docker-compose logs -f api
```

### 5. Nginx reverse proxy (опционально)

Если вы используете внешний nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:80;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # SSL конфигурация...
}
```

## Отладка

### Проверка API

```bash
# Проверка health endpoint
curl http://localhost:8000/api/health

# Получение товаров
curl http://localhost:8000/api/products?page=1&limit=5
```

### Логи Docker

```bash
# Все логи
docker-compose logs -f

# Только API
docker-compose logs -f api

# Только webapp
docker-compose logs -f webapp
```

### Проблемы и решения

**Проблема:** Mini App не открывается в Telegram

**Решение:**
- Проверьте, что WEBAPP_URL правильно настроен
- В production убедитесь, что используется HTTPS
- Проверьте, что webapp контейнер запущен: `docker-compose ps`

**Проблема:** Товары не загружаются

**Решение:**
- Проверьте, что API сервер запущен: `curl http://localhost:8000/api/health`
- Проверьте логи API: `docker-compose logs api`
- Убедитесь, что PostgreSQL запущен и доступен

**Проблема:** Ошибка CORS в браузере

**Решение:**
- В режиме разработки используйте proxy в vite.config.js
- В production убедитесь, что nginx правильно проксирует запросы к API

## MVP Функционал

Текущая версия включает:

✅ Отображение списка товаров с изображениями
✅ Пагинация (20 товаров на страницу)
✅ Адаптивный дизайн для мобильных устройств
✅ Интеграция с Telegram темой
✅ Отправка событий в бот при клике на товар

## Следующие шаги (после MVP)

- [ ] Детальная страница товара
- [ ] Добавление в корзину из Mini App
- [ ] Поиск и фильтрация товаров
- [ ] Избранное
- [ ] История заказов
- [ ] Профиль пользователя

## Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Vue.js 3 Guide](https://vuejs.org/guide/introduction.html)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [aiogram 3 Documentation](https://docs.aiogram.dev/en/latest/)
