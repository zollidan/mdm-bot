# 🚀 Быстрый запуск Mini App

## Шаг 1: Установка зависимостей

```bash
# Python зависимости (FastAPI, uvicorn)
uv sync

# Frontend зависимости
cd webapp
npm install
cd ..
```

## Шаг 2: Настройка .env

Добавьте в ваш `.env` файл:

```env
WEBAPP_URL=http://localhost
```

## Шаг 3: Запуск (выберите один вариант)

### Вариант A: Docker Compose (рекомендуется)

```bash
# Запустить все сервисы
docker-compose up --build

# Остановить
docker-compose down
```

Сервисы будут доступны:
- 🤖 Telegram Bot: работает внутри контейнера
- 🌐 Web App: http://localhost
- 📡 API: http://localhost:8000
- 🗄️ PostgreSQL: localhost:5432

### Вариант B: Локально (для разработки)

```bash
# Терминал 1 - База данных
docker-compose up postgres meilisearch

# Терминал 2 - API сервер
python api_server.py

# Терминал 3 - Telegram бот
python main.py

# Терминал 4 - Frontend dev server
cd webapp
npm run dev
```

## Шаг 4: Проверка

1. Откройте вашего Telegram бота
2. Отправьте `/start`
3. Нажмите "🛍️ Открыть каталог"
4. Вы должны увидеть список товаров с пагинацией

## Проверка API

```bash
# Health check
curl http://localhost:8000/api/health

# Получить товары
curl http://localhost:8000/api/products?page=1&limit=5
```

## Что дальше?

Смотрите [MINIAPP_GUIDE.md](MINIAPP_GUIDE.md) для подробной документации.
