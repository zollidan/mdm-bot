# Quick Start Guide

## 🚀 Production Development за 3 шага

### Шаг 1: Настройка окружения

```bash
# Скопируйте production template
cp .env.production .env

# Отредактируйте .env
nano .env
```

**Обязательно заполните:**
- `BOT_TOKEN` - от @BotFather
- `POSTGRES_PASSWORD` - сгенерируйте: `openssl rand -base64 32`
- `MEILI_MASTER_KEY` - сгенерируйте: `openssl rand -base64 24`

### Шаг 2: Запуск

```bash
make prod
```

Или без make:
```bash
podman-compose up -d
# или
docker-compose up -d
```

### Шаг 3: Проверка

```bash
make health
make logs-bot
```

## ✅ Готово!

Бот работает. Откройте Telegram и отправьте `/start`.

---

## 💻 Для локальной разработки

```bash
# Быстрый старт
make dev

# Посмотреть логи
make logs-bot
```

---

## 📦 Импорт товаров (опционально)

```bash
# Положите old_db_lite.csv в корень проекта
make db-import
```

---

## 🛠️ Полезные команды

```bash
make help          # Все доступные команды
make status        # Статус контейнеров
make restart-bot   # Перезапустить бота
make backup        # Создать бэкап БД
make stop          # Остановить всё
```

---

## 📚 Дополнительная документация

- [DEPLOYMENT.md](DEPLOYMENT.md) - Полное руководство
- [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md) - Чеклист
- [Makefile](Makefile) - Все команды (make help)

---

## ⚠️ Troubleshooting

**Бот не запускается?**
```bash
make logs-bot  # Смотрите логи
```

**Ошибка подключения к БД?**
```bash
make health    # Проверьте сервисы
```

**Нужно всё пересобрать?**
```bash
make down
make dev-build
```

---

## 🆘 Нужна помощь?

1. Проверьте [DEPLOYMENT.md](DEPLOYMENT.md) - раздел Troubleshooting
2. Откройте issue в репозитории
3. Проверьте логи: `make logs`
